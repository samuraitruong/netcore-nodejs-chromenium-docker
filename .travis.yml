services:
  - docker
before_install:
  - docker version

script:
- echo "run docker build"
- docker build -t netcore-nodejs-chromenium -f Dockerfile
deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master
